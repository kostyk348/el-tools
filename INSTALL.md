# Установка Electrical Tools

## Какую версию файлов брать

| AutoCAD | Папка | Кодировка |
|---|---|---|
| **2015 и новее** (включая 2024) | `lsp/` | UTF-8 (BOM) |
| **2014 и старее** | `lsp_cp1251/` | Windows-1251 |

AutoCAD 2014 читает `.lsp` в системной кодировке (на русской Windows — CP1251)
и не понимает UTF-8 BOM. Поэтому для 2014 берите файлы из `lsp_cp1251/`
(в них символы `²` и `→` заменены на `2` и `>`).

> ВАЖНО: не смешивайте папки — загружайте файлы только из одной.

## Установка (любая версия)

1. Скопируйте папку `lsp/` (или `lsp_cp1251/` для 2014) на диск,
   например: `C:\AutoCADTools\el-tools\`.
2. В AutoCAD выполните команду `APPLOAD`.
3. Выберите `electrical-tools.lsp` → «Загрузить».
   Загрузчик сам подтянет все остальные файлы из той же папки.
4. Чтобы загружать автоматически при старте:
   - в окне APPLOAD → «Содержимое» (Startup Suite) → «Добавить» →
     выберите `electrical-tools.lsp`.

## Проверка

```
EL-STATS        ; должна вывести статистику чертежа
```

Если команда не найдена — файлы загружены из разных папок или
загрузчик не нашёл соседние .lsp (загружайте по одному, начиная с el-core.lsp).

## Список команд

`EL-TRACE`, `EL-WHATIF`, `EL-CHECK`, `EL-TABLE`, `EL-GRAPH`, `EL-STATS`,
`EL-LOOPS`, `EL-BOTTLENECK`, `EL-COLOR-CHAINS`, `EL-HOTSPOTS`,
`EL-CROSSREF`, `EL-SAVE-SNAPSHOT`, `EL-DIFF`,
`AW33`, `AW`, `DrawWire`, `WireTable`, `WireNodes`, `WireSegAddr`, `WT`.

Подробности — в README.md.
